# Nexus OS — local dev secrets. Copy to .env and fill in. Never commit .env.
# The key is read server-side by the Vite proxy (vite.config.js) and is never
# shipped to the browser bundle. Get a key at https://console.anthropic.com.
ANTHROPIC_API_KEY=sk-ant-your-key-here
